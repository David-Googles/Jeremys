// =============================================================
// Passcode lock for dashboard homepage.
// Disabled by default - remove the early return to enable.
// To enable: change `false` to `true` below
// =============================================================
(function () {
  'use strict';

  if (false) { // ← LOCK DISABLED - set to true to re-enable
    return;
  }

  // (lock code removed - see git history if needed)
})();